# ปรับปรุงการใช้งาน Builder — 12 กันยายน 2026

พัฒนาจาก GitHub doctornon/examprompt commit eddf72e บน branch improve-builder-workflow

## สิ่งที่เปลี่ยน
- Builder 2: เพิ่มพื้นที่จัดการผังด้านบน พร้อมปุ่มบันทึก โหลด และทางลัดไปยัง prompt
- ตรวจไฟล์ผังเวอร์ชัน 2 ให้ครบก่อนเปลี่ยนฟอร์ม: จำนวนข้อ ตัวเลือก กลุ่มที่ต้องเลือกหนึ่งค่า และ AI/แพ็กเกจ/รุ่น
- ปฏิเสธไฟล์เกิน 1 MB และแสดงข้อผิดพลาดโดยรักษาค่าปัจจุบัน
- จำนวนข้อจำกัดเป็นจำนวนเต็ม 1–50 พร้อมข้อความช่วย และปิดการส่งออกเมื่อค่าผิด
- เปิด AI ภายในเหตุการณ์คลิกเพื่อหลีกเลี่ยงป๊อปอัปถูกบล็อกหลังรอคลิปบอร์ด พร้อมรายงานผลคัดลอกตามจริง
- Builder 1: แก้ fallback คัดลอกที่เคยแสดงสำเร็จแม้คัดลอกไม่ได้ และแก้ลิงก์ Perplexity
- ทั้งสอง Builder: แถบทางลัดมือถือ ฟอร์มตัวอักษร 16px ปุ่มกดขนาดเหมาะสม focus ที่เห็นชัด และ toast สำหรับ screen reader
- Builder 2: เพิ่มโครงสร้าง HTML มาตรฐานและภาษา th

เนื้อหาทางการแพทย์ ข้อกำหนด prompt และทะเบียน AI ไม่ได้เปลี่ยนแปลง

## การตรวจสอบ
- ทดสอบใน Chromium ที่รันด้วย agent-browser
- การตรวจผังและจำนวนข้อ 45 assertions ผ่าน รวมไฟล์ผิดรูปแบบ การคงค่าฟอร์มและ prompt และ round trip ภาษาไทย
- ทดสอบคลิปบอร์ดถูกปฏิเสธและ fallback โยน exception: รายงานล้มเหลวถูกต้อง
- Builder 1 และ Builder 2 สร้าง prompt ได้ ไม่มี page errors ที่เครื่องมือรายงาน
- หน้าจอ 390×844 ไม่มี horizontal overflow; ตรวจ Builder 2 ที่ 1440×1000 เพิ่มเติม
- เมนู admin ยังคงซ่อนสำหรับผู้ใช้ที่ไม่มีสิทธิ์
- หน้าแรกเปิดได้ และ git diff --check ผ่าน

ทดสอบฟอร์ม local โดยซ่อน login overlay ชั่วคราวในเบราว์เซอร์ทดสอบ ไม่ได้เปลี่ยน auth.js และไม่ได้ทดสอบ Google OAuth หรือการบันทึกฐานข้อมูลจริง การตรวจแบบ mechanical ยังพบข้อสังเกตในสไตล์เดิม เช่น contrast บางสีและเงา จึงไม่ถือว่ารอบนี้เป็น accessibility audit ครบทั้งเว็บ

## ทดลองและนำไปใช้
เปิดด้วย static HTTP server เช่น `python3 -m http.server 8765` แล้วไปที่ `/builder1.html` หรือ `/builder2.html` การล็อกอินต้องใช้ origin ที่ได้รับอนุญาตใน OAuth configuration

ไฟล์ runtime ที่เปลี่ยนมี builder1.html, builder2.html และ builder-ux.css ต้องนำไปด้วยกัน
ยังไม่ได้ push, merge หรือ deploy ขึ้น production

## ทดสอบซ้ำ
เปิด Builder 2 บน local server ด้วย agent-browser จากนั้นรัน:

```
agent-browser eval --stdin < tests/builder2-browser.js
```

## รอบที่ 2 — ปรับ prompt ตาม AI และเครื่องมือในแชท

เพิ่มแผง “ปรับคำสั่งให้ตรงกับแชทที่ใช้” ใน Builder 1 และ 2:
- ระบุแหล่งอ้างอิง: ยังไม่ยืนยันค้นเว็บ / เปิดค้นเว็บแล้ว / เอกสารที่แนบในแชท
- ระบุ Thinking/Reasoning หรือโหมดตอบปกติ และจำกัดจำนวนข้อต่อรอบ
- คำสั่งเปลี่ยนจริงตามตัวเลือก พร้อมสรุปแผนรอบและนโยบายอ้างอิง
- สร้างคำสั่งรอบถัดไปและคำสั่งทบทวนสำหรับใช้ในแชทเดิม
- ผัง JSON ของ Builder 2 เก็บ session settings; ผัง v2 เดิมที่ไม่มี settings ยังโหลดได้ โดยกลับไปค่าไม่ยืนยันค้นเว็บ

แก้เครื่องยนต์กลางเป็น cap-2026.09-2:
- ไม่อนุมานว่าค้นเว็บได้จากชื่อผู้ให้บริการ
- แผนแบ่งรอบและข้อความ prompt ใช้เพดานเดียวกัน รวมข้อจำกัด 3 ข้อต่อรอบสำหรับรูปแบบข้อมูลกับโปรไฟล์ loose
- ระบุจำนวนรอบสุดท้าย และแยกจำนวนรวมออกจากจำนวนที่ต้องส่งในรอบนี้
- ไม่สั่งเปลี่ยน JSON/CSV เป็นตารางเอง และไม่แทรกรายงานตรวจนอกโครงสร้าง
- ใส่ task ใน prompt แบบ Markdown เช่นเดียวกับ XML
- Builder 1 ใช้ instructions/reminder ที่คืนจาก engine โดยตรง ไม่แยกด้วย regex
- ลดข้อความที่เคยรับประกันความเทียบเท่าของข้อสอบข้ามโมเดล

### ผลตรวจรอบที่ 2

- Node test suites 5/5 ผ่าน: ทุก provider/plan/model ที่มีในทะเบียนคงชั้นเนื้อหาเดิมและแผนไม่เกินเพดาน, session overrides, invalid settings, unknown models, continuation
- Browser regression 50 assertions ผ่าน: เพิ่มกรณี session ผิดรูปแบบ, ผังรุ่นเก่า และ round trip settings
- ตรวจด้วย Codex in-app browser: 7 ข้อ JSON → 3+3+1 ทั้งคำสั่งและหน้าจอ, การเลือกเอกสารแนบ, ปุ่ม continuation/review และปิดปุ่มเมื่อจำนวนผิด
- Builder 1 เปลี่ยนนโยบายใน prompt เมื่อเลือกค้นเว็บจริง; ไม่พบ console errors ระหว่างตรวจ
- ตรวจแผงใหม่ที่ mobile 390×844 และ desktop 1440×1000
- การตรวจ mechanical ของ ai-session.js และ CSS ไม่พบรายการเตือน

นี่เป็นการตรวจการประกอบ prompt และ UI ยังไม่ใช่การทดลองสร้างข้อสอบจริงกับโมเดลต่าง ๆ ไม่ได้อัปเดตหรือรับรองความพร้อมใช้งานของรายชื่อรุ่นเดิม และไม่ได้เรียก API ของ AI หรือแก้ฐานข้อมูลจริง

### เอกสารที่ใช้ประกอบการออกแบบ

หลักการกำหนดเป้าหมายให้ชัด แยกข้อกำหนดออกจากวิธีทำงาน และทดสอบผลซ้ำ อ้างอิง:
- OpenAI: https://developers.openai.com/api/docs/guides/latest-model?model=gpt-5.5
- Anthropic: https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/overview
- Google: https://ai.google.dev/gemini-api/docs/prompting-strategies

เอกสารเหล่านี้เป็นแนวทาง ไม่ใช่หลักฐานรับรองตัวเลขข้อต่อรอบหรือคุณภาพข้อสอบในทะเบียนเดิม

## รอบที่ 3 — เปรียบเทียบ prompt สองรุ่นใน Builder 2

เพิ่มส่วน “ลองผังเดียวกันกับ AI สองรุ่น” พร้อมทางลัดจากด้านบน:
- ฝั่ง A ใช้รุ่นและการตั้งค่าจากฟอร์ม ฝั่ง B เลือก provider/plan/model ได้
- สร้างคำสั่งทั้งสองจากสำเนาผังเดียวกัน โดยไม่เปลี่ยนค่าฟอร์มหลัก
- แสดงตารางค่าที่ใช้ปรับคำสั่ง และคัดลอก prompt แต่ละฝั่งได้
- กรณีคำสั่งเหมือนกันจะแจ้งตรง ๆ โดยไม่สรุปว่าผลจากโมเดลจะเหมือนกัน
- เมื่อเปลี่ยนผัง รุ่น หรือ session settings ผลเก่าจะถูกซ่อน ต้องสร้างใหม่ก่อนคัดลอกหรือส่งออก
- ดาวน์โหลด JSON ชุดทดลอง: ผัง, preset, session settings, prompt version, prompt ทั้งสองฝั่ง, แผนแบ่งรอบ และเกณฑ์ทบทวนร่วมกัน ผลประเมินเริ่มว่าง

การตรวจรอบนี้:
- Node tests 7/7 ผ่าน รวมการรักษาเนื้อหาเดียวกัน ไม่แก้ข้อมูลต้นทาง และ snapshot ของชุดส่งออก
- Browser regression 50 assertions ผ่านหลังแยก promptSpec ออกมาใช้ร่วมกัน
- ตรวจคัดลอก A และ B โดยเทียบข้อความใน clipboard กับช่อง prompt จริง
- ตรวจการซ่อนผลเก่า/ปิดปุ่มเมื่อจำนวนข้อผิด และกรณี prompt ทั้งสองเหมือนกัน
- กดดาวน์โหลดจริงและอ่านไฟล์ JSON กลับ ยืนยัน prompt ทั้งสองครบ จำนวนข้อ 7 และไม่มีผลประเมินที่แต่งขึ้น
- ตรวจ desktop 1440×1000 และ mobile 390×844; หน้ามือถือไม่มี horizontal overflow; ไม่พบ console errors
- ใช้ fixture แยกสำหรับ UI tests โดยไม่เรียก auth/profile/usage logging; ไม่ได้แก้ระบบล็อกอินของเว็บจริง

ยังไม่ได้ส่งคำสั่งไปทดลองกับ AI ภายนอก และยังไม่ได้ push/merge/deploy รุ่นนี้ขึ้น Vercel
ไฟล์ runtime ใหม่สำหรับรอบนี้คือ compare.js และต้องนำไปพร้อม builder2.html, capabilities.js, ai-session.js และ builder-ux.css ของรุ่นล่าสุด
